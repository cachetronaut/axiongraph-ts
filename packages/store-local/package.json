{
  "name": "@axiongraph/store-local",
  "version": "0.0.0",
  "private": true,
  "description": "Internal: zero-service GraphStore adapters (in-memory + node:sqlite). Published as the `axiongraph/store-local` subpath of the `axiongraph` package.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@axiongraph/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
