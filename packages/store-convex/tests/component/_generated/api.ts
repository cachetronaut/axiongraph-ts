// Marker only (see tests/convex/_generated/api.ts). Lets `convex-test` root the component's
// module tree here — at a test-local shim — so the shipped `component/` dir needs no `_generated`.
export {};
